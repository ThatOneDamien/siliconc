/// test:   valid
/// action: run
/// result: pass
fn main() -> int
{
    int[4] a = [0, 1, 2, 3];
    for(int i = 0; i < 4; ++i)
    {
        if(a[i] != i)
            return 1;
    }
    return 0;
}
