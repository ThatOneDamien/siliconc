/// test:   array_list 
/// action: run
/// result: pass
fn main() int
{
    const a: [4]int = [0, 1, 2, 3];
    var i: usize = 0;
    while i < a.len {
        if a[i] != i {
            return 1;
        }
        i++;
    }
    return 0;
}

/// test:   inferred_array
/// action: run
/// result: pass
fn main() int
{
    const a: [*]int = [0, 1, 2, 3, 4, 5];
    var i: usize = 0;
    if a.len != 6 { 
        return 1; 
    }
    while i < a.len {
        if a[i] != i {
            return 1;
        }
        i++;
    }
    return 0;
}

/// test:   struct_list 
/// action: run
/// result: pass
struct Test
{
    a: int,
    b: float,
    c: ulong,
    d: bool
}

fn main() int
{
    const t: Test = {
        a: 1,
        b: 1.2f,
        c: 0xFAFFFFFFFFFFFFFF,
        d: true,
    };
    return 0;
}
