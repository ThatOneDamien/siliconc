/// test:   eval_int
/// action: compile
/// result: pass
#assert(10 + 16 == 26, "Fail");
#assert(110 - 15 == 95, "Fail");
#assert(12 * 14 == 168, "Fail");
#assert(100 / 10 == 10, "Fail");
#assert(13 % 4 == 1, "Fail");
#assert((0x5 << 4) == 0x50, "Fail");
#assert((0xFF00 >> 4) == 0xFF0, "Fail");
#assert((cast<uint>(0x80000000) >>> 3) == 0xF0000000, "Fail");
#assert((0xC & 0xB) == 0x8, "Fail");
#assert((0xC ^ 0xB) == 0x7, "Fail");
#assert((0xC | 0xB) == 0xF, "Fail");
#assert(cast<uint>(-1) == 0xFFFFFFFF, "Fail");
#assert(cast<uint>(~1) == 0xFFFFFFFE, "Fail");
#assert(!(false), "Fail");
#assert(!(10 == 100), "Fail");
#assert(true && true, "Fail");
#assert(true || false, "Fail");
#assert(128 == 0x80, "Fail");
#assert(1000 != 1, "Fail");
#assert(-20 <= 500, "Fail");
#assert(500 <= 500, "Fail");
#assert(40 >= -5, "Fail");
#assert(-40 >= -40, "Fail");
#assert(0 < 1, "Fail");
#assert(1 > 0, "Fail");

/// test:   eval_float
/// action: compile
/// result: pass
#assert((1.0f + 4.5f) == 5.5f, "Fail");
#assert((4.5f - 2.0f) == 2.5f, "Fail");
#assert((10.0f * 2.5f) == 25.0f, "Fail");
#assert((10.0f / 4.0f) == 2.5f, "Fail");
#assert((1.0f == 1.0f), "Fail");
#assert((0.1f != 0.2f), "Fail");
#assert(-100.0f <= 0.0f, "Fail");
#assert(20.0f <= 20.0f, "Fail");
#assert(100.0f >= -5.0f, "Fail");
#assert(-10.0f >= -10.0f, "Fail");
#assert(1.0f < 2.0f, "Fail");
#assert(1.0f > 0.0f, "Fail");

/// test:   eval_addr
/// action: compile
/// result: pass
int a;
int* b = &a;

/// test:   int_divide_zero
/// action: compile
/// result: fail
int a = 1 / 0;

/// test:   float_divide_zero
/// action: compile
/// result: fail
float a = 1.0f / 0.0f;
