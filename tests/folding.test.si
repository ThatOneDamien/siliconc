/// test:   eval_int
/// action: compile
/// result: pass
#assert(10 + 16 == 26, "Fail");
#assert(110 - 15 == 95, "Fail");
#assert(12 * 14 == 168, "Fail");
#assert(100 / 10 == 10, "Fail");
#assert(13 % 4 == 1, "Fail");
#assert((0x5 << 4) == 0x50, "Fail");
#assert((0xFF00 >> 4) == 0xFF0, "Fail");
#assert(((0x80000000 as uint) >>> 3) == 0xF0000000, "Fail");
#assert((0xC & 0xB) == 0x8, "Fail");
#assert((0xC ^ 0xB) == 0x7, "Fail");
#assert((0xC | 0xB) == 0xF, "Fail");
#assert((-1 as uint) == 0xFFFFFFFF, "Fail");
#assert((~1 as uint) == 0xFFFFFFFE, "Fail");
#assert(true && true, "Fail");
#assert(true || false, "Fail");
#assert(128 == 0x80, "Fail");
#assert(1000 != 1, "Fail");
#assert(20 <= 500, "Fail");
#assert(-20 <= 500, "Fail");
#assert(40 >= 5, "Fail");
#assert(-5 >= -40, "Fail");
#assert(0 < 1, "Fail");
#assert(1 > 0, "Fail");

/// test:   eval_float
/// action: compile
/// result: pass
float a = 1.0f + 1.0f;
float b = 1.0f - 1.0f;
float c = 1.0f * 1.0f;
float d = 1.0f / 1.0f;
float e = -1.0f;
bool f = 1.0f == 1.0f;
bool g = 1.0f != 1.0f;
bool h = 

/// test:   eval_addr
/// action: compile
/// result: pass
int a;
int* b = &a;

/// test:   int_divide_zero
/// action: compile
/// result: fail
int a = 1 / 0;

/// test:   float_divide_zero
/// action: compile
/// result: fail
float a = 1.0f / 0.0f;
