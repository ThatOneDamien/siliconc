priv extern void printf(ubyte* fmt, ...);

pub uint main(int argc, ubyte** argv)
{
    float[2] b;
    b[0] = 0.0f;
    b[1] = 1.0f;
    float[2] c = b;
    for(int i = 0; i < 2; ++i)
        printf("%f\n", c[i]);
    return 1;
}
