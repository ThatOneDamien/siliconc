// This is a comment, it should not appear in the lexer's output
/* 
 * This is a multiline comment.
 * This should also not appear in the lexer's output.
 */

priv extern void printf(ubyte* fmt, ...);

pub uint main(int argc, ubyte** argv)
{
    ubyte* bruh = "okay";
    int c = 0;
    uint d = 4;
    printf("%d %d\n", c, d);
    d <-> d;
    printf("%d %d\n", c, d);
    return 0;
}
